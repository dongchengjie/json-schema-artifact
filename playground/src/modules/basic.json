{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "t(`user.basic.title`)",
  "type": "object",
  "required": ["name", "age"],
  "definitions": {
    "name": {
      "type": "string",
      "description": "t(`user.basic.name`)"
    },
    "age": {
      "type": "integer",
      "description": "t(`user.basic.age`)"
    }
  },
  "properties": {
    "name": {
      "$ref": "#/definitions/name"
    },
    "age": {
      "$ref": "#/definitions/age"
    }
  }
}
