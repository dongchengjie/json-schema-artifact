{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "t(`user.contact.title`)",
  "type": "object",
  "required": ["phone"],
  "properties": {
    "phone": {
      "type": "string",
      "description": "t(`user.contact.phone`)"
    },
    "email": {
      "type": "string",
      "description": "t(`user.contact.email`)"
    }
  }
}
